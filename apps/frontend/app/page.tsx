import LandingLayout from './(landing)/layout'
import MainHomePage from './(landing)/page'

const page = () => {
  return (
    <LandingLayout>
      <MainHomePage />
    </LandingLayout>
  );
}

export default page
