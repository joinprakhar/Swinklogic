import LandingLayout from "./(frontend)/(landing)/layout";
import LandingPage from "./(frontend)/(landing)/page";

export default function Page() {
  return (
    <LandingLayout>
      <LandingPage />
    </LandingLayout>
  );
}
